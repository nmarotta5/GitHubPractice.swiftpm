import SwiftUI

struct ContentView: View {
    @State var answer = ""
    var body: some View {
        VStack {
            Button("Multiply"){
                Text("answer")
            }
        }
    }
}
